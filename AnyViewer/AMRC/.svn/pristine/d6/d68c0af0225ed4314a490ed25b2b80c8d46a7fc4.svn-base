#pragma once

#include "Region.h"

class CWin8DuplicationListener
{
public:
  // m_objChangedRegion in target CFrameBuffer coordinates.
  virtual void OnFrameBufferUpdate(const CRegion *m_objChangedRegion) = 0;
  // pDstRect, nSrcX, nSrcY in target CFrameBuffer coordinates.
  virtual void onCopyRect(const Rect *pDstRect, int nSrcX, int nSrcY) = 0;

  virtual void onCursorPositionChanged(int x, int y) = 0;
  virtual void onCursorShapeChanged() = 0;

  // 开始复制
  virtual void OnStartCapture() = 0;

  // 结束复制
  virtual void OnEndCapture() = 0;


};

