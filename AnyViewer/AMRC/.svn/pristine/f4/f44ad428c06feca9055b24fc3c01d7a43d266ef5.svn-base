#pragma once

#include "CommonHeader.h"
#include "Rect.h"

class CRectSerializer
{
public:
  // Converts to string value.
  static void toString(const Rect *rect, CStringStorage *strOut);

  // Converts to Rect value.
  static Rect toRect(const CStringStorage *strIn);
};